// swift-tools-version: 6.0

import PackageDescription

let package = Package(
    name: "<%= module_name %>",
    platforms: [.macOS(.v12)],
    products: [
        .library(
            name: "<%= module_name %>",
            type: .dynamic,
            targets: ["<%= module_name %>"]
        ),
    ],
    targets: [
        .target(
            name: "<%= module_name %>"
        ),
    ]
)
