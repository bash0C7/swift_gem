import Foundation

// TODO: Implement a self-contained Swift CLI for <%= gem_name %>.
//
// Run with: swift examples/<%= module_path %>.swift <input>

guard CommandLine.arguments.count >= 2 else {
    FileHandle.standardError.write("usage: swift examples/<%= module_path %>.swift <input>\n".data(using: .utf8)!)
    exit(1)
}

let input = CommandLine.arguments[1]
print(input)
